-- Initiales PostgreSQL-Skript für das Projekt.
-- Die Tabellen selbst werden durch SQLAlchemy erstellt.
-- Diese Datei kann für projektspezifische Initialisierungen erweitert werden.

CREATE EXTENSION IF NOT EXISTS pgcrypto;
